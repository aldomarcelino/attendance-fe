import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ThePhotosScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text>ikan</Text>
    </View>
  );
};

export default ThePhotosScreen;

const styles = StyleSheet.create({});
